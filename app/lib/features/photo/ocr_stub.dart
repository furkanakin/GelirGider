/// Web stub for OCR. ML Kit only runs on iOS/Android.
/// Web users can still upload an image; we just send it to backend for vision-OCR (future).
Future<String> recognizeText(String _) async {
  return '';
}
